# Architecture Workspace

Accepted architecture lives in `accepted/`. Current exploratory synthesis lives in `working/` and is non-authoritative until promoted.

See `decisions/ADR_PROCESS.md` for promotion rules.
