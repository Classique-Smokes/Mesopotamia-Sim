# IMP-0004 inherited verification adaptation ledger

Status: coder-owned candidate evidence complete; fresh independent conformance remains required. This continuation supersedes the unfinished evidence status at recovery commit `1de66c3c02926e3c65818a083229dd421b60ca76`; the historical escalation remains preserved.

Authority: `sfl-v0-slice4-inherited-verification-adaptation-v2-approved`, commit `68fddec788b5cf41db5934c8719a6fd1ce474d32`, plan blob `00c3e64a128fa00e91b99117e321a0e379dd61c5`. Frozen manifests and approved refs remain unchanged.

| Approved mapping | Actual trigger and change | Replacement evidence / limitations |
|---|---|---|
| A: formation headlessness | Adding head role/context types caused the inherited S3-014 type-absence assertion to fail. `HouseholdScenarios.cs` now checks one vacant role per produced H, no installed occupant/transition, and no Household decision or effect from formation. | Historical producer still independently verified. `FormationOracleRejectsAutomaticOccupancy` passes a well-formed occupied-role corruption to the actual Slice-4 oracle and expects rejection. |
| B: exact structural inventory | Adding typed HeadRoles, HeadTransitions, ProvisionRefusals, and nextHeadRole caused S3-093's exact HouseholdState field inventory to fail. `HouseholdStructuralScenarios.cs` adds only these named fields; the exact comparison remains. | WorldState inventory is unchanged. `TypedAuthorityAndPhaseCarriedStateHaveExactInventories` checks each new authority/envelope field and immutability. Actual `CachedNomineeEligible=true` mutation bypasses the nominee gate: outsider appointment commits, the semantic test fails, and the exact S3 structural inventory rejects the hidden field. `Run-Slice4Mutants.ps1` retains the patch, successful build and assertion-failure evidence. |
| C: Household material events | Actual support/dowry now emit material Household events. The historical Slice-3 oracle remains strict for its historical producer scope. `Slice4Oracle.cs` independently validates the bounded S4 material events and rejects other Household material events. | `FundingAndRoleCorruptionsReachIndependentChecker` and `MaterialAndOriginCorruptionMatrixReachesIndependentChecker` submit actual detached corruptions: wrong authority/cost/residual/debit, false contributor tie, wrong private owner/amount, omitted backing/possible participants, arbitrary Household payment, private-transfer attitude contribution, and fabricated aggregate credit. Accepted support/dowry controls pass the same checker. |
| D: commitment provenance | Changing commitment provenance to a closed typed union made the old `.Provenance.Fixture` access fail compilation. The historical oracle now first requires `ProvisionFixtureProvenance` and retains its nonempty fixture assertion. | Endogenous and self origins independently bind exact proposal, actor, H/role/current occupant, request/consent/creation and causal order. Corruption cases reject fixture cross-labels, endogenous/self cross-labels, wrong contributor/role, acceptance-as-request, creation-as-request, missing institutional/private predicates and false proposal identity. `SelfCommitmentPreservesPrivateAuthorization` separately removes each consent at the public boundary, rejects fabricated self-negotiation, and verifies grain-neutral acquisition. X-only actions create no standing commitment. |

All four mapped triggers fired. No additional inherited witness was weakened. Exact diffs from baseline `683183c1b58f82dccdcc776644759fe1c04fe109` are retained in Git and exported with the evidence package for `HouseholdScenarios.cs`, `HouseholdStructuralScenarios.cs`, and `HouseholdOracle.cs`. The exact approved plan has four corresponding A–D mappings; this ledger records their execution, not new approval.

`slice4-results.json` retains every frozen classification and all 270 inherited REQUIRED results; `slice4-fixtures.json` retains initial writes and public producer/action histories. Exact commit/clean-tree, canonical command logs, frozen-ref/blob checks, mutation patches/TRX failures and remote CI identity are packaged at delivery. The older progress archive is historical and is superseded by the complete candidate package. Independent conformance and promotion are not inferred from these coder-owned results.
