# Research Task Packet — Adversarial State-Trajectory Designer

**Task ID:** TRES-0010H  
**Status:** OPEN  
**Context mode:** INDEPENDENT-CONTEXT with repository access / minimally anchored  
**Return:** `research/technical/TRES-0010/returns/TRES-0010H_ADVERSARIAL_TRAJECTORIES_REPORT.md`

## 1. Question

Can you construct small valid worlds/event sequences for which the accepted rules do not determine one clear semantic result, or produce surprising contradictions/absorbing states?

## 2. Why it matters now

Static prose review can miss interactions that only appear when individually valid rules compose.

## 3. Objective

Construct a portfolio of minimal counterexample traces, especially around:

- simultaneous debts/favours/help requests;
- one target responding to multiple proposals;
- NeedsGrain appearing/clearing around conflict;
- debt repayment plus personal transfer;
- gift/help meaning versus favour creation;
- direct marriage + residence + debt/favour;
- accepted response later invalidated;
- strong-like / kinship threshold edges;
- formation-evidence thresholds;
- participation bridge handoff;
- stale recognition during succession;
- Active↔Inactive lifecycle edges;
- predecessor dissolution + lineage freshness;
- multiple automatic reactions in one cycle;
- stable-ID symmetry.

For each trace, ask:

> Can the accepted artifacts predict the authoritative history/result without inventing a rule?

## 4. First-pass source restriction

Read:

- SPEC-SFL-0001;
- assumptions register;
- Stage-3 verification package.

Do not read TRES-0003 through TRES-0009 until after the initial trace set is written.

## 5. Evidence standards

Each reported defect must include:

- initial state;
- exact event/proposal sequence;
- competing plausible interpretations or invariant failure;
- governing clauses.

A weird-but-determined outcome is not automatically a defect.

## 6. Boundaries

Do not propose historical calibration. Do not build/run implementation code. Do not mutate canonical artifacts.

## 7. Required output

Provide at least:

- several attempted traces that resolve cleanly;
- any ambiguous/failing traces;
- severity/disposition under the common schema.

## 8. Authority reminder

Advisory only.
